function add(x,y) {

result = x * y;
return result;
// return is like store the answer into the variable
}

//"theResult" will now become 30
var theResult = add (6,5);
//if no return function, the system will be like forgotten how much the answer is so can't print out
document.write (theResult);